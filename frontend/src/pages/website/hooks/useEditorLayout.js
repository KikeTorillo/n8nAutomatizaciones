/**
 * ====================================================================
 * USE EDITOR LAYOUT (WEBSITE) - DEPRECADO
 * ====================================================================
 * @deprecated Usar `useEditorLayout` de `@/components/editor-framework` directamente.
 *
 * Este archivo se mantiene para backward compatibility.
 * La lógica ha sido movida al editor-framework centralizado.
 *
 * @since 2026-02-04
 */

// Re-export desde el framework
export { useEditorLayout, PANEL_TYPES } from '@/components/editor-framework';
