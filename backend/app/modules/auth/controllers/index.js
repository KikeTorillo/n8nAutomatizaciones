/**
 * @fileoverview Exportaciones de controllers del módulo Auth
 * @description Re-exporta todos los controllers de autenticación
 */

const AuthController = require('./auth.controller');

module.exports = {
    AuthController
};
