/**
 * usePropertiesState - Re-export desde editor-framework
 *
 * Este archivo mantiene compatibilidad con imports existentes
 * mientras usa la implementacion del framework compartido.
 */
export { usePropertiesState } from '@/components/editor-framework';
